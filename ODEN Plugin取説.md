# ODEN Plugin 取扱説明書

放置したメモ（具材）を「熟成」させ、今のノートと掛け合わせて新しいアイディア（出汁）を作るObsidianプラグイン。
ここでは、実際にプラグインを触ったときに画面に出てくる文言の意味と、使い方をまとめる。

## 0. 初期設定の手順（最初にこれだけやる）

### 設定画面の開き方
2通りある。
1. Obsidianの設定 → 左メニュー「コミュニティプラグイン」→「インストールされたプラグイン」内の**ODEN**の行にある歯車アイコン（⚙️ 設定）をクリック
2. または、Obsidianの設定 → 左メニューの一番下の方に出てくる「ODEN」を直接クリック

どちらも同じ設定画面（本書「5. 設定画面」の内容）が開く。

### 最初にやること（順番）
1. **Embeddingプロバイダを選ぶ**（ローカル/Ollama、またはOpenAI）
   - Ollamaを選んだ場合：事前にOllama本体をインストールし、`ollama serve`でサーバーを起動、`ollama pull nomic-embed-text`等でモデルを取得しておく（PCを再起動するたびにOllamaは手動起動 or 自動起動設定が必要）
   - OpenAIを選んだ場合：APIキーを入力欄に貼り付ける
2. **「Ollama接続テスト」ボタンで疎通確認**（Ollamaの場合）。「Ollama接続OK」の通知が出ればOK
3. **出汁ブレンド用のプロバイダも選ぶ**（OpenAIまたはClaude）。使う予定がなければ後回しでも良い
4. 設定はフィールドに入力した時点で自動保存される。**保存ボタンや再起動は不要**
5. コマンドパレット（Ctrl/Cmd+P）から「Vaultを再インデックス」を実行する。**これをやらないと、熟成サイドバーには何も表示されない**
6. 再インデックスが終わったら（「ODEN: 完了（更新◯件／スキップ◯件／失敗◯件）」の通知が出る）、任意のノートを開いて熟成サイドバーを開けば候補が表示される

### 設定を変更したときの注意
- **Embeddingプロバイダを切り替えた場合**（例: Ollama→OpenAI）は、次回の「Vaultを再インデックス」で全ノートが再計算される（ベクトル空間の互換性がないため）。切り替えたら必ずもう一度再インデックスを実行する
- モデル名やAPIキーだけ変えた場合も、変更後は再インデックスを一度実行しておくと確実
- **デフォルトのAPIモデルはGPT-4o-mini（出汁ブレンド用プロバイダ＝OpenAIの場合）、またはClaude Haiku 4.5（出汁ブレンド用プロバイダをClaudeに切り替えた場合）です。** ユーザー側でモデルを変更することは可能ですが、モデルによって課金額が変わるため、料金体系を確認の上で変更してください。なお、このプラグインの利用にはAPI課金が発生します（Embedding計算・出汁ブレンド生成のたびに、選択したプロバイダの従量課金がかかります）。課金が発生することをご了承の上でご利用ください

## 0.5 APIが実際に呼ばれるタイミング

「いつ課金対象の通信が発生するか」を明確にしておく。

| タイミング | 内容 |
| --- | --- |
| 「Vaultを再インデックス」実行時 | **変更のあったノートごとに** Embedding APIが呼ばれる（未変更ノートはスキップされ呼ばれない） |
| 「Ollama接続テスト」ボタン | テスト用の短い文字列を1回だけEmbedding APIに送信 |
| 「出汁ブレンドを生成」実行時 | 選択したノート本文をまとめてChat AI（OpenAI/Claude）に1回送信 |

**熟成サイドバーを開く／ノートを切り替えて表示を更新するだけではAPIは呼ばれない**（キャッシュ済みのEmbeddingを参照するだけ）。また、ノートを編集・保存しただけでも自動でAPIが呼ばれることはなく、その変更を反映させるには手動で「Vaultを再インデックス」を実行する必要がある（現状、保存時の自動追従機能は未実装）。

## 1. 熟成サイドバーの見方

リボンの鍋アイコン、またはコマンド「熟成サイドバーを開く」で開く。
アクティブなノートを開いている状態で、そのノートと相性の良い「放置メモ」が一覧表示される。

### 🍢 熟成鍋
サイドバーのタイトル。今開いているノートに対する提案一覧、という意味。

### 「〇〇と相性の良い、煮込み中のメモ」
今アクティブなノートの名前が入る。まだ提案は確定ではなく「候補」というニュアンス。

### 煮込み具合（ステータスラベル）
放置日数 ÷ 設定した「放置日数のしきい値」の比率で、3段階に変化する。

| ラベル | アイコン | 意味 | 目安（しきい値を`T`日とした場合） |
| --- | --- | --- | --- |
| コトコト煮込み中 | 🍢 | まだ提案対象になったばかり | 放置日数が `T`〜`2T` 日 |
| 味がしみ込み中 | 🍲 | ある程度寝かせて、内容が練れてきた状態 | 放置日数が `2T`〜`4T` 日 |
| 食べごろ！ | 🔥 | 十分に放置され、見直すのに一番良いタイミング | 放置日数が `4T` 日以上 |

※そもそも放置日数が「放置日数のしきい値」未満のノートは、候補にすら出てこない（まだ出汁が出ていない扱い）。

### 放置◯日
そのノートが最後に更新されてから何日経っているか。数字が大きいほど長く寝かせている。

### 進捗バー（オレンジのバー）
上表の各ステータス内での進み具合を表す。バーが満タンになると次のステータスに移行する。「食べごろ！」は常に満タン表示。

### 類似度◯%
今開いているノートと、そのメモのEmbedding（AIによる意味ベクトル）がどれだけ近いかを0〜100%で表示。
**数値が高いほど内容的に関連性が高い**、という意味。100%に近いほど「ほぼ同じ話題」ということ。

- 目安として、80%以上ならかなり近い話題、50〜80%は緩やかに関連、50%未満はあまり関係ない可能性が高い
- 類似度が計算されていない（サイドバーに出てこない）場合は、後述の「Vaultを再インデックス」を実行済みか確認する

### 「熟成した関連メモが見つかりませんでした」
以下のいずれかの状態。
1. まだ一度も「Vaultを再インデックス」を実行していない
2. 実行はしたが、今開いているノートと十分似ている「放置済み（しきい値以上経過）」のノートが存在しない

## 2. コマンド一覧（Ctrl/Cmd+P から実行）

| コマンド名 | 何が起きるか |
| --- | --- |
| 熟成サイドバーを開く | 右サイドバーに熟成サイドバーを表示する |
| Vaultを再インデックス | Vault内の全ノートのEmbeddingを計算し直す。**変更のないノートはスキップされる**ので、通常は差分だけが処理される |

## 3. 出汁ブレンド（複数ノートの合成）

ファイルエクスプローラで2つ以上の`.md`ノートを選択 → 右クリック →「出汁ブレンドを生成（◯件）」。

処理の流れ：
1. **ODENプラグイン**が、選んだノートの本文をまとめて取得
2. それを、設定画面「出汁ブレンド用のプロバイダ」で選んだ外部AI（OpenAIまたはClaude）に**APIとして送信**し、共通点や組み合わせ企画のテキストを生成させる
3. ODENプラグインが、その結果を新しい`.md`ノートとしてVault内に作成し、自動で開く

つまり「Obsidianが自身の機能でAI生成する」のではなく、**ODENプラグインが外部のAI（OpenAI/Claude）のAPIを呼び出して**文章を作り、それをノートとして保存している。ノート本文は選択した範囲でAPIに送信されるため、社外秘の内容を含むノートをブレンドする際は使用するAIプロバイダのデータ取り扱いポリシーを確認すること。

### 通知文言の意味

| 通知 | 意味 |
| --- | --- |
| ODEN: 出汁ブレンドを生成中... | AIへの問い合わせ中。生成が終わるまで自動では消えない |
| ODEN: 出汁ブレンドを生成しました「〇〇」 | 新しいノート（〇〇という名前）が作られ、自動で開く |
| ODEN: 出汁ブレンドの生成に失敗しました。〇〇 | AI側のエラー。APIキー未設定・レート制限・接続エラーなどが多い。末尾のエラーメッセージ本文を確認する |

## 4. 再インデックスの通知文言

| 通知 | 意味 |
| --- | --- |
| ODEN: Vaultの再インデックスを開始します... | 処理開始の合図 |
| ODEN: 完了（更新◯件 / スキップ◯件 / 失敗◯件） | 更新＝新規にEmbeddingを計算したノート数、スキップ＝前回から変更がなく計算不要だったノート数、失敗＝エラーになったノート数 |
| ODEN: ◯件のノートでEmbedding計算に失敗しました。 | 失敗件数のサマリー。個別の原因は下記「よくある失敗原因」を参照 |

### よくある失敗原因

- **Ollamaサーバーが起動していない**：`net::ERR_CONNECTION_REFUSED`が出る。`ollama serve`でローカルサーバーを起動する
- **ノートの内容がモデルの上限トークン数を超えている**：Ollamaのログに `input (N tokens) is too large to process` と出る。`nomic-embed-text`はデフォルトのコンテキスト長が2048と短いため、長めのノートで頻発しやすい。環境変数 `OLLAMA_NUM_CTX` を大きくして（例: 8192）Ollamaを再起動すると解消する
- **APIキー未設定/期限切れ**（OpenAI/Anthropicを使っている場合）：設定画面のAPIキー欄を確認する

## 5. 設定画面（ODEN 設定）の項目

### Embedding（類似度計算）
| 項目 | 内容 |
| --- | --- |
| Embeddingプロバイダ | 類似度計算に使うAIを選択。ローカル(Ollama)、または OpenAI |
| OpenAI APIキー / Embeddingモデル | プロバイダがOpenAIのときのみ使用 |
| Ollama ベースURL | Ollamaを起動しているエンドポイント（通常 `http://localhost:11434`） |
| Ollama Embeddingモデル | 事前に`ollama pull <モデル名>`で取得しておく必要あり（例: `nomic-embed-text`） |
| Ollama接続テスト | 短いテキストを実際に送って、接続とモデルの動作を確認するボタン |

**プロバイダを切り替えると、次回の再インデックスで全ノートが再計算される**（ベクトル空間に互換性がないため）。

### 出汁ブレンド（アイディア生成）
| 項目 | 内容 |
| --- | --- |
| ブレンド用プロバイダ | 出汁ブレンド生成に使うAI（Embeddingとは別設定）。OpenAIまたはClaude(Anthropic) |
| OpenAI Chatモデル / Anthropic APIキー・Claudeモデル | 選んだプロバイダに応じて入力 |

### 熟成サイドバー
| 項目 | 内容 |
| --- | --- |
| 放置日数のしきい値 | この日数以上更新されていないノートを「熟成済み」として候補に含める。この日数（`T`）が、上記「煮込み具合」の基準にもなる |
| 表示件数の上限 | サイドバーに出す候補の最大数 |
| 除外フォルダ | インデックス対象から外すフォルダをカンマ区切りで指定（前方一致）。例: `Templates/,Archive/` |
| Vaultを再インデックス（ボタン） | コマンドと同じ処理をここからも実行できる |

## 6. 運用上のヒント

- **APIキーは平文保存**（暗号化されない）なので、共有PCでは注意
- Ollamaを使う場合、PC起動のたびに`ollama serve`が自動起動していないと「接続テストに失敗」が出る。常用するならOllamaをスタートアップ登録するか、`OLLAMA_NUM_CTX`と合わせてタスクスケジューラ等で自動起動させておくと良い
- 除外フォルダにテンプレートやアーカイブを入れておくと、無関係な提案やAPI利用料の節約になる
